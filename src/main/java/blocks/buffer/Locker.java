package blocks.buffer;

// object for blocking resolution
public class Locker {
    // allows to read more than we have with limited buffer size

    //allows to write more than we can with more buffer size
}
